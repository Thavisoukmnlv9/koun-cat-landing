# Left commented out, as it was in the boilerplate. Uncomment to enable — these
# are the same four commands the project is verified with locally. The former
# `e2e` job is gone along with Playwright.

# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:

# jobs:
#   verify:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 26
#           cache: npm
#       - run: npm ci
#       - run: npm run lint
#       - run: npm run typecheck
#       - run: npm run test
#       - run: npm run build
